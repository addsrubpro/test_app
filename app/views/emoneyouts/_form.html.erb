<%= form_for(@emoneyout) do |f| %>
  <% if @emoneyout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@emoneyout.errors.count, "error") %> prohibited this emoneyout from being saved:</h2>

      <ul>
      <% @emoneyout.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :clearing_date %><br />
    <%= f.date_select :clearing_date %>
  </div>
  <div class="field">
    <%= f.label :transaction_id %><br />
    <%= f.text_field :transaction_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for(@requeststatus) do |f| %>
  <% if @requeststatus.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@requeststatus.errors.count, "error") %> prohibited this requeststatus from being saved:</h2>

      <ul>
      <% @requeststatus.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

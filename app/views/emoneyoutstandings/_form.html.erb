<%= form_for(@emoneyoutstanding) do |f| %>
  <% if @emoneyoutstanding.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@emoneyoutstanding.errors.count, "error") %> prohibited this emoneyoutstanding from being saved:</h2>

      <ul>
      <% @emoneyoutstanding.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br />
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

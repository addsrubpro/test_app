<h1>Listing accounttypes</h1>

<table>
  <tr>
    <th>Description</th>
    <th>Default charge rate</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @accounttypes.each do |accounttype| %>
  <tr>
    <td><%= accounttype.description %></td>
    <td><%= accounttype.default_charge_rate %></td>
    <td><%= link_to 'Show', accounttype %></td>
    <td><%= link_to 'Edit', edit_accounttype_path(accounttype) %></td>
    <td><%= link_to 'Destroy', accounttype, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Accounttype', new_accounttype_path %>
